# Integrity, Safeguarding, and Compliance

The current working baseline is:

`institutional_integrity/institutional_integrity_v0_4/`

See the family-level lifecycle map:

`institutional_integrity/LIFECYCLE_STATUS.md`

v0.4 identifies itself as a **pre-filing working package** requiring appropriate board/counsel/CPA review before filing or adoption.

The lifecycle map distinguishes:

- unversioned predecessor/general framework artifacts;
- historical v0.2/v0.3 work and releases;
- the current v0.4 working baseline;
- and the immutable v0.4 release snapshot.

Earlier versions and releases are retained for historical/provenance purposes. Do not silently update older releases to match v0.4, and do not infer legal adoption, filing, or implementation merely from the existence of a current working file.
