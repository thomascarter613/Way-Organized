# Digital Platform

Application source lives in:

`community-platform/`

The app includes reconstructible dependency metadata (`package.json`, `bun.lock`) and its own application documentation.

Generated dependency/build trees such as `node_modules/` and `.next/` are not authoritative repository artifacts and should remain untracked.

The digital platform implements community functionality; code placement does not itself establish religious, doctrinal, or governance authority.
